services:
  postgres:
    image: postgres:16
    container_name: qerp-postgres
    environment:
      POSTGRES_DB: qerp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d qerp"]
      interval: 10s
      timeout: 5s
      retries: 5

  java-service:
    build:
      context: .
      dockerfile: java-service/Dockerfile
    container_name: qerp-java-service
    environment:
      SPRING_PROFILES_ACTIVE: local
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: qerp
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      SERVER_PORT: 8080
      JWT_SECRET: qerp-local-compose-secret-qerp-local-compose
      MARKET_DATA_ENABLED: "true"
      MARKET_DATA_SOURCE_MODE: DEMO_SYNTHETIC
      MARKET_DATA_POLL_MS: 15000
      RESEARCH_ARTIFACTS_PATH: /app/artifacts
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080"
    volumes:
      - ./python-research/artifacts:/app/artifacts:ro

  prometheus:
    image: prom/prometheus:v3.3.1
    container_name: qerp-prometheus
    volumes:
      - ./infra/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    depends_on:
      - java-service
    ports:
      - "9090:9090"

volumes:
  postgres-data:
